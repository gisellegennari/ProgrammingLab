class Funzioni 
   def __init__(self: a<=b<=c)
      a<=b<=c == a<=b<=c
      
      if(a<=b,c>b)
      elise(a>b, c<a and c>b)
      else
      
        if sum(a+b == d and d<b)
        else dif(a+b < c)
        return = 0
        
        stamp (def
